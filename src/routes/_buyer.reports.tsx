import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/_buyer/reports")({ component: () => <div className="p-8 text-center text-hmuted">🚧 Raporlar — yakında</div> });
