import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/buyer/account")({ component: () => <div className="p-8 text-center text-hmuted">🚧 Hesap — yakında</div> });
