import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/buyer/orders")({ component: () => <div className="p-8 text-center text-hmuted">🚧 Siparişler — yakında</div> });
