import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/buyer/messages")({ component: () => <div className="p-8 text-center text-hmuted">🚧 Mesajlar — yakında</div> });
